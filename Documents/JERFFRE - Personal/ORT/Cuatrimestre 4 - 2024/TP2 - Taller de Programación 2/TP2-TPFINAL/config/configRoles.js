export const ROLES = {
    ADMIN: "admin", // ver todos los productos (y por id), crearlos, actualizarlos y borrarlos.
    EDITOR: "editor", // ver todos los productos (y por id), crearlos y actualizarlos.
    VIEWER: "viewer", // ver todos los productos (y por id).
  };

  